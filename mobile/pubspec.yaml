name: ffms_mobile
description: FFMS - Pocket Family Finance & Mobility Companion App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^2.8.0
  intl: ^0.19.0
  shared_preferences: ^2.3.0
  provider: ^6.1.2
  lucide_icons: ^0.339.0
  camera: ^0.11.0
  image_picker: ^1.1.2
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
